#!/usr/bin/python3
import os
import argparse
import sys
import time
from dotenv import load_dotenv

from Monitors import Monitors
from Discord import Discord

# if sys.argv[1] == '-h' or len(sys.argv) < 2:
#     print('./Program.py webhook_env')
#     sys.exit()

env = "FEED_WEBHOOK_URL"
# bsTypeId = None

# env = sys.argv[1]

# load_dotenv()
# webhook_url = os.getenv(env)

def main():
    print("Running the application")

# executes the main function by running the program
main()


